// app.js — OkObserver (v1.33.9-lite)
const APP_VERSION = "v1.33.9-lite";
window.APP_VERSION = APP_VERSION;
console.info("OkObserver app loaded", APP_VERSION);

// ... full working code would be here (as pasted earlier) ...
