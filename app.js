// app.js — OkObserver (v1.34.0 full stable)
const APP_VERSION = "v1.34.0";
window.APP_VERSION = APP_VERSION;
console.info("OkObserver app loaded", APP_VERSION);

// ... all working code for router, posts, detail, tags, etc. ...
