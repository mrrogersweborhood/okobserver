name = "okobserver-proxy"
main = "worker.js"
compatibility_date = "2024-10-01"
workers_dev = true

# If you later map a custom domain, add a route here:
# routes = [
#   { pattern = "proxy.okobserver.org/*", zone_id = "<YOUR_ZONE_ID>" }
# ]
